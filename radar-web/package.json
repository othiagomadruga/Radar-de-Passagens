{
  "name": "radar-passagens-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute radar --remote --file=schema.sql"
  },
  "devDependencies": { "wrangler": "^4.20.0" }
}
